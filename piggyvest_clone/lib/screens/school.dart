import "package:flutter/material.dart";


class School extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('School')
      ),
      body: Center(child: Text('School boring School'),),
    );
  }
}